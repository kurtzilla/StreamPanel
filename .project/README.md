# `.project`

Workspace for **project-related notes, scratch ideas, and misc files** that support the work but are **not** application source. Tracked in git so the team can share context; content does not ship with the product unless you explicitly move it into `src/`.
