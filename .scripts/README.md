# `.scripts`

**Build helpers, automation, and one-off tasks** (shell, PowerShell, batch, etc.) that make development easier but **do not ship** with the runtime app. Prefer small, documented scripts; link from root [`README.md`](../README.md) or [`docs/`](../docs/) when a script is part of the regular workflow.
